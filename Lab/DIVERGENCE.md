# Lab divergence log

Every intentional Lab deviation from the frozen demo gets a dated row. A divergence "graduates" only when PRD.md or DESIGN.md is amended to match — until then it ships nowhere.

| Date | What diverged | Why | Status |
|---|---|---|---|
| 2026-07-19 | Lottie celebration overlays (confetti, Gullnese shimmer, GM-steal sting, themed Mål landmarks) replace the demo's placeholder CSS confetti + synth-only celebration. | Implements DESIGN.md §4 (Nesen/Gullnese), §5 (Board ceremony), §7 (motion & sound grammar), which the pre-DESIGN prototype predates. Flow, pacing, and scoring are unchanged — canonical. Reduced Motion falls back to static (no overlay). | Graduated (DESIGN.md §4/§5/§7) |
| 2026-07-19 | HOME (title) + RULES/Regler + ABOUT screens added ahead of the demo; flow now opens on HOME (with an inline NO/EN toggle) before the language step; in-game `?` help + setup rules link reach RULES. | Implements PRD §8 screens inventory (Home · Regler · About/Credits) and the §7/§11 onboarding goal. No scoring or round-flow change — engine vectors untouched. | Graduated (PRD §8) |
| 2026-07-19 | Game-feel pass: theatrical WebAudio sound grammar (gong+riser on vote-open, wah-wah GM steal, rising boing per nose notch, truth chord, win fanfare) replacing placeholder beeps; a screen flash + gong on "Åpne avstemning"; a screen shake on GM steal; the Monk mascot reacts (suspicious while others bluff, delighted on the winner). | Realises the PRD §11 success bar ("the Åpne avstemning press gets a reaction every round") and DESIGN.md §7 sound/motion grammar. Event names unchanged (LANES seam #4). No scoring/flow change. All RM-guarded. | Graduated (PRD §11, DESIGN.md §7) |
| 2026-07-25 | **Online rooms.** The Lab gains a host/join flow (screens 19 Vertens lobby, 20 Bli med, 22 Venter på verten, 23 Mistet kontakten): the host shares a 6-character code or a link, players join from their own device, and the host starts the game. WebRTC peer-to-peer behind the transport seam in `Lab/js/net.js` (vendored PeerJS, MIT); a public broker handles signalling only and never sees game content. The host is authoritative and broadcasts state through `netProject()`, which strips the truth, other players' drafts, and live vote attribution per recipient. | PRD §2.1 amendment (2026-07-25) puts online play in scope **for the web build only** — the frozen demo and the iOS app are single-room by design, and `Sources/` gains no networking. Realises what `Specs/ONLINE-PLAY.md` always specified as "a third transport". Scoring and round flow unchanged; engine vectors untouched. | Graduated (PRD §2.1) |
| 2026-07-25 | **Phase timers.** Four countdowns — bløffing 60 s, lokkemat 45 s, avstemning 45 s, avsløring 25 s per beat — on by default online and in practice, off in hotseat, lengths set by the host on screen 06. At 0:00 the game advances: a missed bluff never enters the option pool, a missed decoy opens voting anyway, a missed vote simply doesn't count. | PRD §5.2a amendment, resolving the §13 open question as `Specs/FLOW.md` **Option B**. The demo has no clock because it never had to survive one player walking away from a phone in another building. Timeout is per-round and explicitly *not* a drop (§5.5). Engine stays timerless — expiry is an ordinary host-fired action, proven by new vectors D4/R8/R9/E7–E9. | Graduated (PRD §5.2a, §13) |
| 2026-07-25 | **Career rating + profile (screen 21).** An Elo-style rating, games played, wins and career nose count persist in `localStorage` and are exchanged inside a room so everyone present sees everyone's rank. The About copy no longer claims the app stores nothing; the profile screen carries a "Slett profilen" button. | PRD §2.1 + §10. The demo is stateless between games, which is right for a single device but leaves nothing to be proud of across an evening. Per-device storage keeps the iOS "Data Not Collected" label untouched (PRD §10) — but the web build's privacy line changes, and saying so plainly is part of the change, not a footnote. | Graduated (PRD §2.1/§10) |
| 2026-07-26 | **Late join: a 3-minute door.** The room stays open for `NET_CONFIG.LATE_JOIN_MS` after the host presses start. A latecomer takes a **bot's chair** by preference — inheriting its score, which the banner states out loud — and only gets an extra seat if no bot is sitting. Arriving after the card is drawn puts them in `timedOut` for that round: they watch it out and play from the next. Past the window, or once the game is won, the host answers `bye/started` and the joiner lands back on screen 21 with the play-vs-bots way out. | Fixes a dead end rather than adding a feature: a client that connected after `startGame()` was seated in `NET.peers` but never in `G.players`, so `netBroadcastState` skipped it (`seat < 0`) and the person sat on screen 22 forever. Deliberately **not** matchmaking — the code or link is still required, so PRD §2's "no matchmaking with strangers" holds and no moderation surface opens. Reuses `timedOut` (vectors D4/E9: per-round, explicitly not a drop) rather than inventing a second exclusion concept. | **Pending** — PRD §5.5 covers disconnects but says nothing about joining a game in progress; needs a §5.5 amendment |
| 2026-07-26 | **Back navigation.** Every screen but HOME carries a `←` in the topbar, and the phone's own back gesture is captured (`popstate`, re-armed each pop) so it navigates the app instead of leaving it. Mid-game it raises an "Avslutte spillet?" overlay; leaving a room closes the peer connection and clears any live deadline. Screen parents are a declarative map, not a history stack. | The demo is a one-way funnel — once you pick a mode you cannot get out without a reload, which on a phone means losing the game. A party game people hand around needs an exit. No stack because 40+ sites assign `U.screen` directly and threading one through them is a refactor no gate would catch a miss in; the map is the same shape `U.rulesReturn` already used. | **Pending** — PRD §8 lists the screens but specifies no navigation model |
| 2026-07-26 | **Munken redrawn — one mark, every avatar.** `LOGO()` becomes a bevelled cream token with a hard offset shadow, flat brows, dot eyes, a tilted smirk and a full-capsule Nose lying across the face and jutting ~50 % of a head-width past the rim (was: a plain circle, a symmetric smile arc, and a half-capsule Nose bolted to the outer edge). `.face`/`.nose` in `components.css` are now that same drawing, with all six moods retuned; every dimension is a ratio of one `--fs` custom property and the ink outline is an inset ring rather than a `border`, so the features hold their proportions from 34 px to 68 px. The six hand-rolled avatar call sites in `ui.js` collapse into one `face()` helper — two of them had already drifted and were rendering with no mouth at all. Decorative faces wear `.nose.brand` (logo length); faces whose Nose reports a vote count stay on the `--notch` scale. | Executes DESIGN.md §4: the Nose is where the boldness budget is spent, and the mark now actually spends it — the old half-capsule read as a spout, not a lie. Also removes the drift risk that made the mouth vanish on two screens, per the §4 requirement that the Monk be one recognisable character everywhere. No flow, scoring or copy change; engine vectors untouched, 71/71 tests green, 23 PNGs re-snapped. | **Pending** — DESIGN.md §4 describes the Nose's *behaviour* but pins no mark geometry; needs a DESIGN amendment (or a decision to keep geometry out of DESIGN) before it graduates |
| 2026-07-19 | Boards render DESIGN.md §3 art direction beyond the demo's simplified MVP: Salongen wood-grain + lamp pool + inlaid discs, Fjellet paper-cut ridges + cairn discs, Verdensrommet nebula + parallax starfield + orbit-ring discs; routed track polyline; gold Mål medallion; letterpress hero type; tinted "piece" shadows; the Nesen now actually boings (mount keyframe) and five defined-but-unused spring tokens wired to their moments. | Fulfils DESIGN.md §3 (three boards), §4 (Nesen), §5 (board ceremony), §7 (motion grammar) and §9 (focus rings, 44 px targets, lifted secondary-text contrast). Pure CSS/SVG, token-pure, Reduced-Motion-safe. No engine change. | Graduated (DESIGN.md §3/§4/§5/§7/§9) |
